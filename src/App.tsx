import ErrorBoundary from 'Components/common/error-boundary';
import Layout from 'Components/layout';

const App = () => (
    <ErrorBoundary>
        <Layout>App</Layout>
    </ErrorBoundary>
);

export default App;
